# Security hardening: disable npm lifecycle scripts to prevent supply chain attacks
# This protects CI from malicious preinstall/postinstall scripts in dependencies
# For local development, use: npm install --ignore-scripts=false
ignore-scripts=true
# Only resolve dependency releases that have aged for at least two weeks.
min-release-age=14
